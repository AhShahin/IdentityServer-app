@model List<WebClient.Models.User>
@{
    ViewData["Title"] = "User";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Users</h1>
<table>
@foreach(var item in Model)
{
    <tr>
        <td>@item.FirstName</td>
        <td>@item.LastName</td>
    </tr>    
}
</table>